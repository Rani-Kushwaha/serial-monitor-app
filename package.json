{
  "name": "serial-monitor-app",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . SerialMonitor --platform=win32 --arch=x64 --out=dist --overwrite"
  },
  "dependencies": {
    "electron": "^25.0.0",
    "serialport": "^10.5.0"
  },
  "devDependencies": {
    "electron-packager": "^17.1.1"
  }
}